# Python 編譯產物。
#
# 這些檔案內嵌產生它們的那台機器上的原始檔絕對路徑，而且綁定特定的 CPython
# 版本，對使用者沒有任何用處。2026-08-03 曾有 47 個 .pyc 一路走到準備推送的
# 狀態才被攔下——當時發版腳本的九段驗證全部通過，沒有任何一段在看「發出去的
# 東西裡有沒有多的」。
__pycache__/
*.pyc
*.pyo

# 產生報告的預設輸出（真的在這個目錄跑過工具時才會出現）
pve-network-audit-*.txt
