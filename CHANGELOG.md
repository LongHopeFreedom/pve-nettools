# 變更紀錄 — pve-nettools

負責人：LeeFreedom（秉迅資訊 BingXun InfoTech）

版本號採 `XX.XXX.XXX` 慣例。**現行版以 `./pve-network-audit --version`（Python 入口）
為準**，與本檔最上方的版本必須一致；改版時兩處 MUST 同步。

> [CHANGE] 2026-08-02：這條「MUST 同步」以前只寫在這裡靠人記得。實測發現 Python
> 版 v03.000.000 上線後，本檔最上方仍停在 v02.002.001——**規則寫著卻沒有任何東西
> 會變紅**。已補上 `tests/test_changelog_version.py`，兩處不一致即測試失敗。

> 本檔於 2026-07-29（v02.000.002）自腳本檔頭獨立出來。此前變更紀錄與程式碼放在
> 同一個檔案裡，隨版本累積會讓檔頭愈長，而檔頭是每次開檔第一眼會看到的地方。
> 獨立後腳本檔頭只留「這支工具是什麼、怎麼用、誰負責、改動前要知道什麼」，
> 歷史沿革查本檔。

---

## v03.013.000（2026-08-05）

### `[REMOVE]` 公開版不再隨附 Bash 版

`pve-network-audit.sh`（Bash v02.002.001）自本版起**不再發布到公開儲存庫**。

| 面向 | 處置 |
|---|---|
| 公開儲存庫最新版 | 移除該檔 |
| 公開儲存庫既有歷史 | **不改寫**——`v03.012.000` 以前的提交裡仍然取得得到 |
| 內部儲存庫 `old/pve-network-audit.sh` | **原封不動保留**，繼續作為規格真值與備查來源 |

★ **這個決定的代價逐字記載於此**：先前保留它的理由是「Python 版的真機驗證尚未
通過，Bash 版是唯一完整驗證過的版本」。移除之後，公開版只剩
`REALWORLD_VERIFIED=partial` 的 Python 版——「驗證限度」節列出的未涵蓋情境
**不再有可退回使用的舊版**。此後果在決定前已具名提出，兩份公開 README 的
「版本說明」節也逐字寫著，不以「已經移除」代替告知。

★ 本版**未改動任何 Python 產品邏輯**：改的是版本字串、發版清單與文件。
上一版（v03.012.000）列出的未驗項目**狀態一律不變**，發布本版同樣不會讓
它們變成已驗。

## v03.012.000（2026-08-05）

### ★ 本版發布時的真機驗證狀態（**部分驗證**，逐項具名）

本版於 2026-08-05 在一台 PVE 節點上做了**部分**驗證。已驗與未驗逐項列出，
**未驗的項目不因為發布而變成已驗**：

| 項目 | 狀態 |
|---|---|
| 選單第 25 項版面（ring 六欄、卸載 63 項全列、單位只出現一次） | ✅ 已在真機確認 |
| 選單第 15 項兩段新內容（標題在、原樣輸出不解析不截斷） | ✅ 已在真機確認 |
| **待辦 #67**：報告目錄全程持有父目錄 fd | ❌ **未在真機執行過**。它只在 `report.generate()` 中執行，而該次驗證只看了互動檢視（不寫報告檔）。這是一條只在支援 `dir_fd` 的平台才會走的分支，開發機（Windows）結構上選不到 |
| **待辦 #14**：`LANG=en` 之下不漏中文 | ❌ **未在真機執行過**（需產生報告檔才驗得到） |
| **待辦 #15 的前提**：「PVE GUI 上沒有 compile／localnet」 | ❌ **仍未實測**。若 GUI 其實有，該段功能應撤除 |
| 截斷策略（本版刻意不截斷） | ⚠️ 取得的樣本來自**防火牆停用**的節點，`compile` 僅約 20 行。防火牆啟用時的輸出量級未知，**不能以此樣本判定不需要截斷** |

★ 另有一個**待確認的缺陷候選**：VLAN 對帳區段在「有 bridge 但沒有任何一座開啟
`vlan_filtering`」時，會落到「VLAN-aware Bridge 上沒有可辨識的 Uplink Port」
這一句，而該情境的實際成因是「沒有 VLAN-aware Bridge」。兩者指向的排查方向不同。
未關閉，待下一版處理。

### 新增：防火牆頁補上 GUI 看不到的兩件事（待辦 #15）

PVE 的 GUI（Datacenter／Node／VM → Firewall）給得出的是**設定**；「這份設定編譯
成什麼規則」與「哪個網段被判定為本地」，據我們所知只有 CLI 有。選單第 15 項
因此在既有的 `.fw` 原文**之後**新增兩段：

> ★ **「GUI 上沒有這兩者」尚未在真機 GUI 上實測**（開發機沒有 PVE）。這是依
> PVE 文件與 GUI 結構的推論，已列為真機驗證的確認項。本項的正當性建立在這個
> 前提上，若實測發現 GUI 其實有，這一段要重新評估。

| 子指令 | 回答的問題 |
|---|---|
| `pve-firewall compile` | 這份設定編譯出來的實際規則長什麼樣 |
| `pve-firewall localnet` | 哪個網段被判定為「本地」 |

- **原樣輸出、不解析**，體例同 bash 版印 `.fw` 原文。理由：解析器要寫對得先有
  真機輸出樣本，而原樣輸出不需要——沒有樣本卻定義欄位，等於憑推測決定丟掉什麼，
  而**被丟掉的東西不會有任何症狀**。
- **不截斷**。截斷門檻同樣要真機樣本才定得出來，而靜默截斷比長輸出更糟。
  輸出長度列為真機驗證的確認項。
- 舊版 PVE 若沒有這兩個子指令，該段會明講「無法取得」而不是留白——
  「查不到」與「查了但沒有」MUST 可分辨。
- `pve-firewall` 這支指令本身不存在時**不會**去跑這兩個子指令。理由不是速度
  （實測一次失敗的 subprocess 只花 4ms），而是這是別人機器上的盤查工具，
  明知拿不到還發動 exec 只會在 audit log 留下與問題無關的噪音。

### 修正：`LANG=en` 之下讀不到 conntrack／鄰居表時會漏出中文（待辦 #14）

`render` 層原本把 `collect` 層的 `error` 字串原樣印出，而 `collect` 層結構上
沒有語言概念——它的 error 是硬編字面，一份，不經訊息表。實測 `en` 之下印出的是
「無法讀取 conntrack 核心參數，模組可能尚未載入」。

- 讀不到時的說明改由 `render` 層的訊息表出（新增 `conntrack.unavailable`／
  `neigh.unavailable`，兩種語言）。措辭與語言是 `render` 的職責。
- `collect` 層四處 `"%s：%s"` 的全形冒號改半形：`error` 欄是**語言中立的診斷
  資料**，標點屬於措辭，不該由那一層決定。
- 新增 `tests/test_error_language.py`：① `render` 層讀 `error` 的位置集合不得
  靜默增加 ② 那些位置對應的 `collect` 模組其 error 字面不得含 CJK
  ③ 端到端——以**真的 reader**（指向不存在的路徑，真機上也咬得動）驗 `en`
  輸出零 CJK。其餘模組的中文 error 列為**具名豁免**，而豁免帶有不適用條件：
  一旦某個模組進了 ① 的清單，`test_豁免的不適用條件` 會判紅。

### 修正：報告目錄的 TOCTOU 空隙（待辦 #67）

`mkdir_chain()` 原本建完就把最後一段的 fd 關掉，`write_report()` 再以
`open_dir_chain()` 從根重新解析一次路徑——**中間那段空隙就是 TOCTOU**。

- `mkdir_chain()` 改成 context manager，把 fd **交出去**；新增 `prepared_dir()`
  取代 `choose_makedirs()`，讓 `generate()` 全程持有父目錄 fd 直到寫檔完成。
  路徑因此只解析一次，撞名重試也共用同一個 fd（原本每試一次就重解析一次）。
- ★ 這是一條**只在支援 `dir_fd` 的平台執行**的分支，與 v03.009.000 那個最貴的
  缺陷（#50）同型。故刻意不動注入點的呼叫形態（仍是
  `makedirs_fn(directory, exist_ok=True)`），並讓「三個分支交出來的東西形態一致」
  在開發機上就驗得到。**`os.mkdir` 收不收 `dir_fd` 本身仍只有真機答得出來。**

### 修正：判定詞的射程是散文寫的，而散文沒有守衛（待辦 #69）

`tests/test_i18n_verdict_wording.py` 第一版寫著「VLAN 對帳是整份工具**唯一**會
下判定的區段」——**實測不成立**：`render/netconf.py` 的 autostart 同樣在下判定，
那三個判定詞當時完全在射程外。

- 射程改成從產品碼**掃出來**（`verdict_keys()` 認兩種寫法：指派給 `verdict`
  的 `t()`、名字以 verdict 結尾的函式回傳的 `t()`），再與具名集合對帳。
  新增一個判定詞而忘了配錨，測試會當場紅。
- 判定詞的互異性改成**分組**比較：`autostart.ok` 與 `vlanrec.match` 都是「相符」
  是對的，它們回答的是不同的問題。全體比較會把正確的設計判成紅。

### 修正：發版腳本切節的兩個具名射程限制（待辦 #68）

`realworld_section()` 原本只認 ` ``` ` 圍籬，且圍籬未閉合時節會一路吃到檔尾。

- 兩種圍籬（` ``` ` 與 `~~~`）都認，且**同種字元才閉合**（CommonMark 的規則）。
- 圍籬未閉合時以 rc=3 回報，呼叫端**明講**「射程不可信」並判紅——同 #59，
  不能讓「檔案本身壞了」的輸出與「全部通過」逐字相同。
- 揭露判準自檢 13 → 16 條。

---

## v03.011.000（2026-08-05）

### 修正：兩處會讓工具當場崩潰的數字判斷（待辦 #66）

判斷「是不是數字」用 `str.isdigit()`、轉換用 `int()`——`"²".isdigit()` 是 `True`
而 `int("²")` 直接拋 `ValueError`，於是同一份資料在兩步得到不一致的答案。

| 位置 | 觸發條件 | 後果 |
|---|---|---|
| `render/vlanreconcile.py` 的 tag 排序 | 某台 guest 的 `net0: …,tag=²` | VLAN 對帳整段崩潰 |
| `collect/sysfs.py` 的 `_natural_key` | 介面名如 `eth²` | 網卡清單排序崩潰，位置在整份報告最前段 |

- 產品碼**沒有任何寬泛的 `except`**（實測 `except Exception` 全倉零命中），
  所以這個 `ValueError` 會一路炸穿互動選單與完整報告。
- ★ **可達性未實測**（本棒第 1 輪對抗式覆核自己抓到的過度宣稱）：`tag=²` 要
  手動編輯 `/etc/pve/…/*.conf` 才進得去（`qm set` 應會驗整數）；`eth²` 則是
  依核心 `dev_valid_name()` 的規則**推論**可建立，開發機是 Windows 驗不了。
  兩者都已列入真機驗證手冊的待驗項。
- ★★ 但修法的正當性**不依賴可達性**：判斷用 `isdigit()`、轉換用 `int()`，
  是拿兩套不同的數字判準處理同一份資料，它們對同一輸入給出不一致的答案
  （`"２"` 一個判成數字一個判成非數字）。那本身就是缺陷。
- ★★ 這條規則在倉裡**已經寫了兩遍**（`collect/pve.py` 的 `_DIGITS_RE` 上方、
  `collect/bridge.py:38`），措辭都正確，**而兩份都沒有閘門**。
  ⇒ 規則有 SSOT 不等於規則有守衛。已補 `tests/test_ascii_digit_guard.py`
  以 AST 掃描產品碼，禁用 `isdigit`／`isnumeric`／`isdecimal`。
  其陽性對照用的是**這一棒修改前的真實原始碼**（已知答案 3），不是合成示意句。
- 發現途徑：待辦 #61 複核時讀到 `_tag_key` 的 docstring 宣稱「與
  `collect.pve.guest_vlans` 同規則」，拿同一組輸入餵兩邊才看出它們不同規則。
  **兩份判準各自讀起來都對。**

### 變更：VLAN 對帳改走 collect 層原語（待辦 #61）

- `GuestConfReader.guest_vlans()` 回傳形狀由 `{bridge: [tag]}` 改為
  `{bridge: {tag: [vmid, …]}}`，`render/vlanreconcile.py` 直接使用它。
- 原本 render 層自己重組一份 `_guest_vlans()`，繞過原語的理由寫在註解裡：
  「它另有呼叫端，改它的回傳形狀會波及不相干的地方」——**實測那句是假的**
  （產品碼零呼叫端）。一句假註解讓那個原語從此沒人用，也讓排序被重寫了第二份。
- 移除 `BridgeReader.vlan_allowed()`：它問「單一 port 是否放行」，而唯一的
  對帳場景問的是「一座 bridge 所有 uplink 的聯集」，語意不同、結構上用不到。
- 移除 `NetconfReader.comments()`：2026-08-02 待辦 #26 移除 `nic.comment` 欄
  那一批的漏網殘留，同批的三個輔助函式當時都刪了。
- 以上三項為使用者逐項裁決。`tests/test_public_api_reachable.py` 的已知孤兒
  清單由 4 項降為 1 項（`statistics`，待真機 `ethtool -S` 取樣後接線）。

### 新增守衛

- `tests/test_menu_table_reconcile.py`（待辦 #63）：三份 README 的選單表與
  `MENU_ENTRIES` 對帳（編號集合與群組欄）。名稱**刻意不對帳**——實測 README
  的「項目」欄是刻意簡稱，連前綴關係都不成立（3 處），先寫判準再實測的話
  那 3 處會變成假陽性，而「修好」它們等於把刻意的簡稱改壞。
- `tests/test_readme_claims_internal.py`（待辦 #53）：發版腳本第 9b 段只讀
  `$PUBLIC_DIR` 且只在發版時跑，**內部 README 從來沒有人對帳過**。實地後果：
  它同時寫著「2026-08-05 於 v03.009.000 重跑」與「完整測試 686 條」，而 686 是
  2026-08-03 的量測，同一段下一行又寫著 v03.009.000 是 761 條。已標清歸屬。
  ★ 判準只驗上界並要求「至少一句描述現況」——歷史量測 MUST NOT 被自動更新成
  現況，那會謊稱新版真機驗過。
- `tests/test_i18n_verdict_wording.py`（待辦 #52）：判定詞（`vlanrec.match`／
  `vlanrec.check`）的逐字錨。★ 實測推翻了 #52 的描述：它記的是「zh-TW 那半邊
  沒有判準」，而改 **en** 的訊息文字同樣沒有任何測試會紅——真正的性質是
  **判準的兩邊來自同一張表**（測試普遍用 `t("key")` 當期望值）。
- `tests/test_mkdir_chain.py`（待辦 #60）：逐段 mkdir 的呼叫契約。

### 修正：報告目錄建立不再跟隨 symlink（待辦 #60）

- `generate()` 原本用 `os.makedirs`，它會跟隨 symlink（第 4 輪資安檢測的具名殘留）。
  支援 `dir_fd` 的平台改走新增的 `mkdir_chain()`，逐段建立、每段帶 `O_NOFOLLOW`。
- ★ 這是一條**只在目標平台執行**的分支，正是待辦 #50 那個最貴缺陷的形態。
  故刻意**不動 `makedirs_fn` 的簽名**，並把分支選擇抽成 `choose_makedirs()`，
  讓「三個分支選出來的東西呼叫形態一致」這件事在開發機上就驗得到。
  突變條目另有一條**刻意複製 #50 的缺陷形態**（簽名收不下 `exist_ok`）。

### 修正：發版腳本的兩個盲區（待辦 #58／#59／#64）

- **#59**：執行位元檢查原本是 `if [[ -d .git ]]` 而**沒有 else**，對複本跑
  端到端時整段靜默跳過，輸出與「全部通過」逐字相同。現在會明講「本次未驗證」。
- **#58**：受檢對象由列舉檔名改為**從 shebang 推導**（要守的性質是「這個檔會被
  直接執行」）。同一支函式的行尾檢查早已因待辦 #56 補過涵蓋率對帳，執行位元
  這半邊沒有跟上——**同一個病只修了一半**。另加反向對帳，既有入口若被拿掉
  shebang 會變紅。
- **#64**：`realworld_section()` 切節改為追蹤 ``` 圍籬狀態。原本圍籬程式區塊裡
  以 `# ` 開頭的行（bash 註解、shebang）會被誤認成標題而讓節提早結束。
  揭露判準自檢由 11 條增為 13 條。

---

## v03.010.000（2026-08-05）

### 新增守衛

- `tests/test_no_stray_modules.py`：套件（`pve_nettools/`）與測試（`tests/`）
  之外，專案目錄下不得有任何 `.py`。
  - 成因是真機實地事故：最早那次上傳把套件內容**直接放在 `/root/pve_nettools/`**，
    後來改成巢狀的 `pve_nettools/pve_nettools/`，而**舊檔沒有被覆蓋掉——它不在
    新的上傳清單裡**。於是同一台機器兩個版本共存，`import pve_nettools` 命中
    哪一份**只取決於當下的工作目錄**，且沒有任何徵兆。
    實地後果：在 `/root` 下跑 `--self-test` 得到「v03.000.000、23 checks」。
  - ★★ **唯一看得見它的地方，是 3.9 相容性守門員印的那一行規模**：
    真機「共 134 個 Python 原始檔」、開發機 106——**差的 28 個就是殘骸**。
    那個數字一直印在畫面上，而沒有人去比對它。
    ⇒ **印出規模只做到一半；要有東西拿它跟不變量比，才叫守。**
  - 附合成陽性／陰性對照，以及「具名例外沒有腐爛」的斷言。

### 文件

- 真機驗證手冊新增「步驟 2 之二：檢查有沒有舊版殘骸」，含以 `import` 印出
  `__file__` 與 `__version__` 的釘死法，以及**移走而非刪除**的處置。
- 三份 README 的驗證限度節依 2026-08-05 實測改寫：`--report` 完整報告已在真機
  重跑，**21 個區段全部有輸出**，含「網卡緩衝區與卸載功能」在報告版面
  （固定寬度，與互動版面是不同的一條路徑）的產出，多欄最長行 130 欄未超出上限。
  ★ 「2026-08-04 追加（v03.009.000）」那句**刻意保留原版號**——它是歷史量測，
  不是現況宣告；改成新版號等於謊稱那次驗證是在新版上跑的。

## v03.009.000（2026-08-04）

### 修正（**只在目標平台發作，開發機結構上看不到**）

- **注入的 `opener` 收不下 `dir_fd`，在 Linux 上一律 `TypeError`。**
  待辦 #50 讓 `write_report()` 在支援 `dir_fd` 的平台改以
  `opener(basename, flags, mode, dir_fd=…)` 呼叫，而當時既有的**五個注入點**
  全是三個位置參數。
  - 開發機（Windows）`os.open not in os.supports_dir_fd` ⇒ 那條分支**從不執行**
    ⇒ 761 條測試全綠、突變 339／339 全殺、完整 harness 零存活。
  - 真機（PVE-201，Linux）那條分支**一定執行** ⇒ 七個 `TypeError`，
    其中**三個在 `pve_nettools/selftest.py`——那是產品碼**，
    代表 `--self-test` 在目標平台上是壞的。
  - 影響版本：**v03.006.000 起**（引入 #50 的那一版）。產品的報告產生路徑
    `generate()` **不注入 opener**，故報告功能本身未受影響。
- 五個注入點（`selftest.py` 一個、`tests/test_report.py` 五個中的四個加一個
  既有的）全部改為 `opener(target, flags, mode, dir_fd=None)`。

### 新增守衛

- `tests/test_opener_contract.py`：AST 掃全倉，斷言每一個被當作 `opener=`
  傳出去的函式或 lambda 都收得下 `dir_fd`。
  - ★ 守的是**契約**不是行為——Windows 上無法真的走通那條分支
    （`os.open(..., dir_fd=<int>)` 直接 `NotImplementedError`）。
    行為那一半由真機的 `python3 -m unittest discover`（skipped 應為 0）守。
  - ★ **鑑別力以真實程式碼驗證**：對修正前的原始碼跑，抓到 **6 個**
    （`selftest.py` 1 ＋ `test_report.py` 5）；修正後 0 個。
  - ★ 掃描器自己犯過兩個假陰性，都已釘成合成案例：
    ① 整檔一張 name→def 表 ⇒ 7 個同名 `opener` 只查得到任意一個；
    ② 只看直接子節點 ⇒ 寫在 `with` 區塊裡的 `def` 全部漏掉
    （本倉的注入點幾乎都在 `with tempfile.TemporaryDirectory()` 裡）。

## v03.008.000（2026-08-04）

### 修正（**真機輸出比對後發現**）

- **`ethtool -g` 的未列名欄位不再被丟掉。** 解析器原本只認 `RX`／`RX Mini`／
  `RX Jumbo`／`TX` 四個欄位名，其餘一律忽略。真機（PVE-201 的 r8169）實測另有
  **6 個欄位**，其中 **`TX Push: off` 與 `RX Push: off` 是真值不是 `n/a`**，
  全部被靜默丟掉。現在全數收進 `extra` 並顯示於「其他 ring 參數」一節。
  - 同一個欄位名若 `Pre-set maximums:` 與 `Current hardware settings:` 兩段都
    出現（實測 `TX push buff len` 如此），**以 Current 段為準**——「目前是什麼
    狀態」正是盤查要問的。
  - ★ 這是同一個病的第三次：offload 是 63 取 10、`.gitattributes` 是檔名列舉
    漏掉新檔、這次是 10 個欄位名取 4。**用「我想得到的清單」取代「真實資料的
    形狀」**，而每一次都要真機輸出被人看見才會發現。
  - ★ 對應測試的 fixture **逐字取自真機輸出**。自己編的 fixture 只有我想得到的
    四個欄位名，而缺陷正是「只想得到四個」——編出來的 fixture 對它結構上沒有
    鑑別力。

### 已釐清（非缺陷）

- 真機顯示 `RX 256／256`（目前值＝上限）**是正確的**：該卡的
  `Pre-set maximums: RX: 256`。★ 這份輸出本身無法否證「兩段狀態切換沒生效」的
  假設（兩段的值剛好相同，兩種情況同形）；能排除它的是單元測試以**可區分的
  資料**驗過切換，加上突變條目 `IP`（把 current 段標題當成 max）為 KILLED。

## v03.007.000（2026-08-04）

### 變更（**使用者可見**）

- **卸載功能不再只顯示十項，其餘全部顯示。** v03.006.000 只印十項重點，
  並以一行說明「共 N 項、未顯示 M 項」。真機實測（`enp6s0`）那一行是
  **「共 63 項，此處顯示 10 項，未顯示 53 項」——84% 的資料被藏起來，
  而且沒有任何出口**。
  - 十項重點仍逐行對齊顯示（PVE 網路調校最常查的那些），其餘項目**全部**
    以等寬多欄排在後面，順序沿用驅動輸出的順序（`ethtool` 把子項印在父項旁邊，
    那個分組比字母序好查）。
  - ★ 原設計的理由是「怕洗版」，而那個限制**不存在**：互動輸出本來就走 `less`。
    對一支盤查工具而言，用一個不存在的限制換掉 84% 的資料是反向的。
  - 多欄的寬度上限為 132 欄。報告模式的版面寬度是「不要折行」的哨兵值，
    直接用它算欄數會把所有項目排成一行。

### 修正

- **緩衝區那一行的「（目前值／上限）」印了兩次**。註記移到欄位標籤，
  只出現一次：`緩衝區（目前值／上限）：RX 256／4096，TX 256／4096`。

## v03.006.000（2026-08-04）

### 新增

- **選單第 25 項：網卡緩衝區與卸載功能**。`ethtool -g` 的 RX/TX 環形緩衝區
  （目前值／上限，`RX Mini`／`RX Jumbo` 僅在有值時顯示），以及 `ethtool -k`
  的十項卸載功能。報告區段由 20 個增為 21 個。
  - 顯示的卸載項目是**刻意的封閉集合**，不隨驅動輸出自動擴張；該卡實際解析到的
    項目多於顯示的項目時，會多印一行說明共幾項、顯示幾項、未顯示幾項。
  - `large-receive-offload` 為 `on` 時多印一行提示：LRO 會把收到的封包重組成
    大封包，經由 bridge 或路由**轉發**的流量無法還原成原本的封包序列。這是事實
    陳述不是錯誤——純終端主機上開啟 LRO 是正常的。
  - ring 與 offload 是兩道各自獨立的查詢，一道失敗不影響另一道的顯示。
- `media.aui`／`media.mii` 兩個顯示字串。此前判別邏輯**已經認出**是 AUI 或 MII，
  顯示層卻把結論丟掉印成「未知」——把已知說成未知，比漏判更糟。

### 變更（**行為改變**）

- **報告建檔改用 `O_EXCL`，撞名時自動加序號**（`…-235659-2.txt`）。此前是
  `O_CREAT|O_TRUNC`：檔名是「主機名＋**秒級**時間戳」，同一秒重跑會直接截斷既有
  檔，別人預先建好的檔也會被寫進去。使用者可見行為維持「重跑仍會產出報告」。
  - 成功訊息與回傳值改為**實際寫入的路徑**，不再是傳進去的那一個。
- **報告開檔改為逐段開啟父目錄**（`O_DIRECTORY|O_NOFOLLOW` ＋ `dir_fd`）。
  `O_NOFOLLOW` 只擋路徑的最後一段，父目錄若本身是 symlink 仍會被跟隨。
  `dir_fd` 不被支援的平台（Windows）走原本的整條路徑開啟。

### 修正

- `BondReader.read()` 讀到空檔時，`data` 由空 dict 改為與正常解析**完全相同的
  欄位形狀**。此前兩條路徑的形狀不同，空檔時任何欄位存取都會 `KeyError`。
- `_parse_offload` 的截斷揭露少算未顯示筆數：`shown` 原本恆為清單長度，
  該卡缺少其中幾項時，揭露行本身會說錯數字。

### 移除

- `NetconfReader.autostart()`。它把 `read()` 投影成 `{auto, hotplug}` 而
  **結構上表達不了 unavailable**：讀不到設定檔時回 `auto=[]`，於是每張執行中的
  網卡都會被判成「執行中但未設 autostart」——憑空生出的嚴重警告。
  2026-08-01 起 render 層已改走 `read()`；要 auto／hotplug 請直接讀它。
- `RingOffloadSection.title()` 與其專屬標題字串。區段標題由選單項提供，
  兩者的值在兩種語言下逐字相同，屬同一句話的兩份真值。

## v03.005.000（2026-08-03）

### `[FIX]` 按 Ctrl-C 中斷時不再噴出一整片錯誤堆疊

在報告的分頁畫面按 Ctrl-C 離開，畫面上會留下九層 Python 錯誤堆疊，最底下是
`os.waitpid`。那不是程式出錯——Ctrl-C 是使用者主動中斷——但看起來就像工具壞了。

這支工具從頭到尾都是互動式的：選單等你輸入、分頁器等你翻頁、LED 定位會停住
十秒。**中斷是預期中的操作，不是例外狀況**，因此現在改成印一行「已中斷。」
就離開，離開碼採 shell 慣例的 130。

分頁器那一層另外處理：中斷時會先讓它把終端狀態收拾乾淨（還原畫面、復原終端
設定）再離開，否則畫面會停在殘缺狀態，得自己打 `reset`。連按兩次 Ctrl-C 則
不再等待，直接離開。

### `[DOC]` README 的「驗證限度」改為據實記載已驗與未驗的部分

此前寫的是「尚未在真正的 Proxmox VE 主機上驗證，只有選單項目 1 確認過」。
2026-08-03 已在 PVE 9.2.5（kernel 7.0.6-2-pve）上跑完整份報告，20 個區段全部
產出、實體網卡各欄位取到真實數值、內建自檢與完整測試在該主機全數通過。

但那台主機沒有 Bond、沒有 SFP/QSFP 模組、沒有 VLAN 子介面，三道 ethtool 查詢
也全部成功——**那些路徑一次都沒有被走到**。README 現在把涵蓋範圍與未涵蓋的
情境分開列出，兩者一起讀才不會誤解。

## v03.004.000（2026-08-03）

### `[FIX]` ethtool 讀不到資料時，不再宣稱一批其實正常的欄位是 N/A

工具查一張網卡會跑**兩道各自獨立**的 ethtool 指令：一道問速率與 Duplex，另一道
問驅動與匯流排位址。此前只要其中一道失敗，畫面上那句說明就一律宣告「速率、
Duplex、媒介與驅動資訊將顯示 N/A」——可是另一道其實成功了，那些欄位好端端地
顯示著數值。說明與眼前的畫面對不上，讀的人只能自己猜哪一半是真的。

現在說明只列出**真正受影響的那一組欄位**：速率查詢失敗就說速率與 Duplex，驅動
查詢失敗就說驅動資訊，兩道都失敗才兩者都說。

媒介則從這句話裡拿掉了，有兩個各自獨立的理由：速率查詢失敗時，媒介會改由讀取
模組 EEPROM 判定，**仍然可能判得出來**；而它真的判不出來時，畫面顯示的是
「未知」，本來就不是 N/A。

### `[FIX]` 報告檔的建立方式加嚴，防止被預先放置的符號連結導向其他檔案

報告的檔名是可以事先算出來的（主機名加時間戳），而輸出目錄可用 `REPORT_DIR`
指定。若把它指到其他人也能寫入的目錄，對方就能預先在那個檔名上放一個指向別處
的符號連結——工具建立報告時會跟著連結過去，把對方指定的檔案清空。

現在建立報告時**不跟隨符號連結**（遇到就直接失敗），而且把權限收緊成僅擁有者
可讀寫這個動作，改成對**已經開啟的那個檔案**下手，不再用檔名重新找一次。用檔名
再找一次會留下一個空隙，那段時間裡檔名可能已經指向別的東西了。

預設輸出目錄 `/root` 只有 root 寫得進去，因此預設情形本來就不受影響；這次加嚴
針對的是把輸出目錄改到共用位置的用法。

## v03.003.000（2026-08-03）

### `[FIX]` 外部指令逾時或子行程層級的失敗，不再被說成「未安裝」

上一版把「讀不到 ethtool 資料」的原因分成三句，但**分辨原因的那一層**還是把兩件
不同的事歸成同一種：「指令根本跑不起來」（找不到執行檔、沒有執行權限）與「指令
已經啟動了、是執行過程出的事」（例如逾時）。後者被歸進前者，於是工具會對使用者
說「可能未安裝」——而那支指令其實裝得好好的，只是這一次沒跑完。

現在三者分開：跑不起來維持原本的訊息；**逾時說逾時**；其餘已經啟動過但失敗的
情形走「成因不明」，不再斷言有沒有安裝。工具不知道的事就不說，這是上一版那個
修正的同一條原則，只是這次補在更上游的地方。

### `[ADD]` 外部指令加上時限，卡住的指令不會再讓整份盤查停住

在此之前工具**不會為任何外部指令設時限**。只要有一支指令沒有回應——例如非叢集
主機上的 `pvecm status`、資料庫沒起來時的 `ovs-vsctl`——整份盤查就停在那裡，
只能 Ctrl-C 中斷。而網路盤查工具正是「這台主機有點不對勁」的時候才會被打開的
東西，那也正是外部指令最容易沒有回應的時候。

現在每一支外部指令都有 **15 秒**的時限。逾時會被明說成逾時（上面那四句的第三
句），不會冒充「未安裝」。秒數可用環境變數調整：

```bash
COMMAND_TIMEOUT=30 ./pve-network-audit --report
```

> 在很慢的硬體或很大的叢集上若看到逾時訊息，先把秒數調高再判斷是不是真的有
> 問題——15 這個數字沒有客觀正確值，它只是「足夠正常回應、又不會讓人以為當掉」
> 的折衷。

## v03.002.000（2026-08-03）

### `[FIX]` ethtool 讀不到資料時，不再一律說成「未安裝」

此前只要 ethtool 沒有回傳可用資料，網卡清單下方就固定顯示「未安裝 ethtool」。
但讀不到的原因不只一種：ethtool 真的沒裝、裝了但執行失敗（權限不足、驅動不支援）、
或是取值的那一層沒有交代原因。三種情形被壓成同一句話，於是在後兩種情形下，
工具會對使用者說一件不成立的事——照著去 `apt install ethtool` 也解決不了問題。

現在依實際原因分成三句：

* **跑不起來**（找不到執行檔、檔案沒有執行權限等）→ 「無法執行 ethtool（可能未
  安裝，或檔案沒有執行權限）」。這裡刻意**不斷言「有沒有裝」**——工具在這個情形
  下並不知道，說「未安裝」就是一句沒有根據的話
* **跑起來了但離開碼非 0** → 「ethtool 已安裝但執行失敗（可能是權限不足或驅動
  不支援）」。這一句可以斷言「已安裝」，因為指令確實執行了才會有離開碼
* **原因無法確定** → 「ethtool 資訊讀取失敗，成因不明」

成因取自**每一次** ethtool 查詢。同一張網卡會執行 `ethtool <網卡>` 與
`ethtool -i <網卡>` 兩道獨立的指令，任何一道失敗都會有對應的說明；此前只看第一道，
於是第二道單獨失敗時，Driver 與 PCI 欄位是空的卻沒有任何提示。

多張網卡（或同一張網卡的不同查詢）原因不同時，對應的幾句會同時列出。

### `[ADD]` 內建自檢由 23 項增為 53 項，補上六類

`--self-test` 此前只驗排版寬度、VLAN 展開、guest 欄位解析、介面設定解析與雙語
訊息表五類。新增的六類是「錯了不會有任何徵兆」的那些——輸出看起來完全正常，
只是行為已經不對：

* **sysfs 屬性讀取**：讀不到時要安靜回預設值，不能把錯誤訊息漏到畫面上
* **網路媒介判定**：只讀有語意的欄位，且 RJ45／BASE-T 的判斷要先於線長
  （1000BASE-T 銅口模組的線長可達 100m，順序錯了會被判成 DAC 銅纜）
* **bridge VLAN 輸出解析**：表頭列與縮排續行都不能被當成 port
* **ethtool 呼叫次數**：同一張網卡的多次查詢只會實際執行一次
* **清單截斷**：超出上限時要明說截掉幾筆，不能靜默截斷
* **報告檔權限**：報告一律以 `0600` 建立

## v03.001.000（2026-08-03）

### `[ADD]` 窄視窗的區塊版面補上顏色

視窗寬度不足以容納表格時會自動改用區塊版面。此前區塊版面**完全沒有顏色**——
同一份資訊在寬視窗有顏色、窄視窗沒有。現在區塊標題與資料值都與表格版採用
同一份判色，兩種版面不會再對「紅色代表什麼」給出不同答案。

涵蓋實體網卡、網卡健康度、VM/CT 網卡對應、Bridge VLAN、VLAN 子介面、VLAN 對帳、
OVS、LLDP、介面設定共九處區塊標題。

VM/CT 網卡對應另補上狀態（執行中綠／未執行黃）與 VLAN Tag（實際有 tag 才上色）
的判色，此前這兩欄在兩種版面都是純文字。

### `[ADD]` 互動流程的訊息依語意著色

錯誤（權限不足、找不到實體網卡、無法建立報告目錄或報告檔、選項無效）為紅色；
成功（LED 定位完成、報告已寫入）為綠色；提醒（此網卡或驅動不支援 LED 定位）
為黃色。此前這些訊息全部同色，失敗與成功在畫面上看起來一樣。

報告檔本身**維持完全不含顏色碼**：報告會被 grep、會被貼進工單，多兩個逸出碼
在肉眼上看不出來卻會讓比對失敗。

### `[ADD]` LED 定位的選卡清單補上 MAC 與連線狀態

此前只顯示編號與介面名稱，多張網卡時無法確認選的是哪一張。連線狀態的文字與
顏色與「實體網卡狀態」區段共用同一份判定，不會各自演化。

### `[ADD]` 補上四處該印卻沒印出來的說明

- RX/TX 取樣前的提示——取樣期間畫面會停住數秒，此前沒有任何說明
- RX/TX 的判讀說明（無流量不代表網路異常）
- 未安裝 ethtool 時，說明速率與雙工顯示 `N/A` 的原因
- 內建自檢的 `[SKIP]` 項目顯示**跳過的原因**，此前只顯示項目名稱

### `[CHANGE]` 顯示措辭改以 Python 版為準

bash 版保留備查，不再作為文字模板。功能、欄位集合與顯示順序仍以 bash 版為
對照基準；標題、標點與欄寬 padding 等措辭差異視為 Python 版的自有選擇。

---

## v03.000.000（2026-08-02）

### `[CHANGE]` 由單一 bash 腳本改寫為 Python 套件

進入點改為 `pve-network-audit`（無副檔名，以 shebang 執行），實作放在 `pve_nettools/`
套件內，分為 `collect/`（讀取系統狀態）與 `render/`（輸出呈現）兩個子套件。
只用標準函式庫，不需 pip 或 venv；相容 Python 3.9 以上。

原本的 bash 版保留為 `pve-network-audit.sh`，**尚未退役**。

### `[ADD]` 選單擴充為 25 項，其中 4 項是 bash 版沒有的

`21` sysctl 網路參數、`22` conntrack 連線追蹤容量、`23` 鄰居表容量（ARP／NDP
`gc_thresh`）、`24` 開機自動啟用對帳（auto／hotplug）。

原有的 1～17 盤查項目、18～20 的「依序檢視全部／輸出完整報告／執行內建自檢」
維持不變，欄位、順序與鍵欄寬以 bash 版為準逐欄對齊。

### `[ADD]` 介面雙語（繁體中文／English）

啟動時可選語言，選單內按 `L` 即時切換，也可用 `PVE_AUDIT_LANG=zh|en` 指定；
未指定時依 `LC_ALL` → `LC_MESSAGES` → `LANG` 推導。訊息表兩份、邏輯一份。

### `[NOTE]` 與 bash 版的刻意差異

- 鄰居表以**欄位**判定 REACHABLE／STALE，而非子字串比對——bash 的
  `grep -E 'REACHABLE|STALE'` 會把 `UNREACHABLE` 一併收進來（`REACHABLE` 是它的
  子字串），與畫面上宣稱的「僅列 REACHABLE 與 STALE」自相矛盾。
- corosync 設定檔尾缺 `}` 時保留最後一個節點，不靜默丟棄——「設定檔壞了」與
  「少一台節點」在畫面上同形，而後者會讓人以為叢集真的少一台。
- SDN 排除「只有註解」的 `.cfg`，不印空標題。
- 窄終端多一套區塊版面（欄位與順序完全相同，只是換排法）。

### `[NOTE]` 內建自檢的涵蓋範圍比 bash 版小

Python 自檢目前是 `group_width`／`group_vlan`／`group_guest`／`group_netconf`／
`group_i18n` 五組。bash 版另外涵蓋而 Python 尚未涵蓋的有六項：sysfs 讀取失敗處理、
媒介判定、`bridge vlan show` 解析、`ethtool` 實際呼叫次數、清單截斷的揭露、
報告檔權限。

### `[NOTE]` 尚未在真正的 Proxmox VE 主機上驗證

目前只確認選單第 1 項的畫面與 bash 版一致，其餘項目與所有新增項目都尚未在真機
執行過。首次上線請對照實際輸出逐項確認。

---

## v02.002.001（2026-07-30）

### `[FIX]` LED 定位選單的 Link 沒有顏色

實地回報。該頁直接印 `get_link_plain`（無色版），而其他所有表格都是 `padc` 搭
`link_color` 上色，於是唯獨這一頁的「已接線／未接線」是白的。

順帶把選單產生邏輯抽成 `render_nic_pick_list()`。抽出來的理由是**可測性**：
`identify_nic_led` 本身要 `read` 使用者輸入，自檢無法直接跑它，「有沒有上色」
這件事就永遠沒有人守——這個缺陷能活到被實地回報，正是因為如此。

新增自檢第 16 段：用假 sysfs 造一張已接線與一張未接線的網卡，直接跑選單產生
函式並檢查 ANSI 碼，含「無色模式下 0 個 ANSI 碼」的陰性對照（否則報告檔會被
色碼汙染）。

> 本版是 bash 版的最後一個功能版本。工具正在重寫為 Python（見下方說明），
> bash 版已移至 `old/` 保留備查，待 Python 版真機驗證通過後除役。

---

## v02.002.000（2026-07-29）

### `[FIX]` `bridge-vids 2-4090` 時前面的 VLAN 數字看不到

實地回報：查 VLAN 時若 bridge 設的是 `2-4090`，只看得到最後一小段，前面的 VLAN
數字全部看不到。

根因不是終端也不是分頁，而是**單一欄位塞了一整條 VLAN 清單**。當 `bridge vlan show`
是逐個列出（而非合併成範圍）時，某個 port 的清單會串成 **23432 字元的單行**，在
80 欄終端折行成約 300 行，把表頭與前面所有 port 全部推出畫面。

修法兩層：

1. **`compress_vlan_list()`** — 把連續且標記相同的 VLAN 壓回範圍表示。
   實測 23432 字元 → **10 字元**（`1u,2-4090t`）。只有連續且標記相同才合併，
   `1u,2u,3t` 會壓成 `1-2u,3t` 而不會把 `3t` 併進去。
2. **`wrap_vlan_list()`** — 壓縮後仍過長時（例如 `100t,200t,300t…` 這種不連續的
   清單壓不掉）折行續列，續行對齊欄位起點。

修正後該表最長一行從 23432 字元降到 **90 字元**。

### `[FIX]` VLAN 對帳在大範圍下的成本

對帳原本把 uplink 放行清單展開成逐個值再建關聯陣列鍵。實測 `2-4090`：展開 171 ms
＋ 建 4090 個鍵 586 ms ＝ 約 **757 ms**，而那還只是單一 bridge 單一 uplink 的成本。

改用 `vlan_in_list()` 直接做範圍包含判斷，並在存入前先壓縮清單（19342 字元 → 6 字元
`1-4090`）。對帳實際只需查 guest 用到的那幾個 VLAN，通常不到 20 個。

`expand_vlan_list()` 保留——自檢用它與 `vlan_in_list()` **交叉驗證**：兩份獨立實作
對同一組探針必須給出相同判斷。用範圍比對是為了效能，正確性由逐個展開背書。

### `[ADD]` 互動檢視改走 pager

實測 30 台 VM 時 `bridge vlan` 就 91 行（整理版 34 ＋ 原始輸出 57），100 台會到
300 行左右，直接印會整頁刷過去。互動檢視改交給 `less -SRX`：

- `-S` 長行不折行，改用左右方向鍵**橫向捲動**（寬表格不必犧牲欄位）
- `-R` 保留顏色、`-X` 離開後不清畫面
- 附帶 `less` 內建的 `/` 搜尋——在 30 台 VM 裡找某個 VLAN 特別有用

報告模式（非 tty）與 `NO_PAGER=1` 一律原樣輸出，不經 pager——排進 cron 的
`--report` 不會掛在那裡等人按鍵。

### `[FIX]` `term_width()` 在 pager 下誤判為寬螢幕

接上一項：走 pager 時 stdout 是 pipe，原本的 `[[ ! -t 1 ]]` 會判成報告模式而回
9999，使窄終端的區塊版永遠不會被觸發。改為 stdout 與 stderr **皆非 tty** 才視為
報告情境，寬度也改向 `/dev/tty` 問而非問已被接走的 stdout。

### `[ADD]` 自檢第 16、17 段

第 16 段守 pager：不只驗「有沒有啟用」，更驗**內容有沒有被它改掉**（吞行、改顏色碼、
截斷長行）——一個會吞行的 pager 會讓盤查結果悄悄少東西。
第 17 段守 VLAN 壓縮／折行／範圍判斷，含 `2-4090` 的直接回歸測試與上述交叉驗證。

突變實測：壓縮不合併、折行改為不折、範圍判斷退化為單值比對，三者皆讓自檢變紅。

---

## v02.001.000（2026-07-29）

### `[ADD]` 窄終端自動切換版面

三張寬表格在 80 欄終端會折行，對齊整個毀掉——而 PVE 網頁主控台（noVNC）預設就落在
這個寬度附近，網卡愈多、看得愈久，這問題愈明顯。

實測寬度：

| 版面 | 表格版寬度 | 窄版寬度 |
|---|---|---|
| 實體網卡狀態 | 131 欄 | 69 欄 |
| 網卡健康指標 | 134 欄 | ≤80 欄 |
| VM/CT 網卡對應 | 136 欄 | ≤80 欄 |

新增 `term_width()` 與 `use_table()`：終端夠寬用原本的表格，不夠寬自動改用逐張區塊
（欄位垂直排列，永遠不會折行）。可用 `TERM_WIDTH` 環境變數強制指定。

**報告檔不受影響**：輸出不是 tty 時一律視為寬螢幕。同一份報告在不同機器上讀，
內容必須一致，版面不該由「產生當下的終端多大」決定。

### `[FIX]` 區塊版的尾隨空白

區塊版每行最後一個欄位原本也做了補白，留下看不見的尾隨空白。改為行末不補白。
另把兩行超過 80 欄的說明文字折短（`render_nic_health` 81 欄、`render_guest_nics`
84 欄——**超長的是說明散文，不是資料行**）。

### `[REFACTOR]` 抽出 `_load_nic_health()` 與 `_guest_iface_state()`

表格版與區塊版共用同一份取值邏輯，避免兩套實作各自取值而漂移——那正是 v01 的
`show_*` / `report_*` 兩套碼的老問題。

### `[ADD]` 自檢第 15 段：版面切換

驗 `TERM_WIDTH` 覆寫、`use_table` 的閾值判斷、非 tty 恆用寬版，並**用假 sysfs 實際
跑一遍兩種版面量寬度**——只驗 `use_table` 的布林值守不住「render 有沒有真的照它
切換」，而版面正是使用者會看到的東西。

突變實測：`use_table` 恆真、拿掉非 tty 判斷、窄版改呼叫表格版，三者皆讓自檢變紅。

---

## v02.000.002（2026-07-29）

文件結構調整與檔頭資訊補齊，**不涉及任何盤查行為的變更**。

### `[CHANGE]` 變更紀錄自腳本檔頭獨立為本檔

原本變更紀錄與程式碼同檔，43 行且會隨版本持續增長。移出後檔頭空間改放不隨版本
增長的內容。

⚠ 誠實記錄：檔頭**並沒有變短**（仍為 73 行）——移出 43 行變更紀錄，換進 43 行的
作者資訊、依賴、權限、驗證限度與設計要點。改變的是**它不再隨版本無限增長**，
不是「瘦身」。

### `[ADD]` 檔頭補上作者資訊與改動前必讀事項

依既有慣例補上「用途／負責人／建立日期／變動日期／版本」，並新增「設計要點」
五條——都是實測踩過才寫下的，改動對應段落前必讀：

1. 媒介判定只讀結構化欄位值，MUST NOT 對整份 `ethtool -m` 輸出做關鍵詞比對
2. 讀 sysfs MUST 判「讀取是否成功」而非「檔案是否可讀」
3. 表格對齊 MUST 用 `pad()` / `str_width()`，MUST NOT 用 `printf %-Ns`
4. ethtool 快取 MUST 由迴圈直接呼叫 `prime_nic_cache()` 預熱
5. 清單輸出 MUST 用 `print_limited()`，MUST NOT 直接 `head -N`

### `[ADD]` 自檢第 14 段：版本字樣三處同步

版本字樣現在散落在三處（腳本檔頭、`VERSION` 變數、本檔最上方）。三處分離後最可能
發生的就是改了其中一處而另兩處留在舊版，而**這種漂移不會有任何 rc 變紅**，只會讓
後人讀到互相矛盾的版本。故由 `--self-test` 實際比對三者。

突變實測：改檔頭版本、改 `VERSION` 變數、改本檔最新版本，三者皆會讓自檢變紅；
本檔不存在時標為 `[SKIP]` 而非靜默通過。

---

## v02.000.001（2026-07-29）

對 v02.000.000 做最後審查時抓到三項，**其中第一項是 v02.000.000 自己的假宣稱**。

### `[FIX]` ethtool 快取從未生效

v02.000.000 的變更紀錄寫著「改為每張卡每種輸出各取一次並快取」——**那句話是錯的**。

快取寫在 `ethtool_out()` / `ethtool_drv()` 內，而它們全部是以 `$(ethtool_out "$nic")`
形式被呼叫。command substitution `$( )` 會開 subshell，subshell **繼承**的變數可以讀，
但它對變數的**寫入不回傳父行程**，所以快取一次都沒命中。函式邏輯與 `[[ -n "${CACHE[$k]+x}" ]]`
判斷都完全正確，**從程式碼結構上看不出任何問題**。

用計數用的假 `ethtool` 實測（量測基準）：

| 版本 | 每張網卡的 ethtool 呼叫次數 |
|---|---|
| v01.000.000 | 7 |
| v02.000.000（宣稱已快取） | **6** ← 幾乎沒改善 |
| v02.000.001 | **3**（RJ45 電口主機 2 次，且完全不讀 SFP EEPROM） |

改法：新增 `prime_nic_cache()`，在每個逐卡迴圈中**直接呼叫**（不可包在 `$( )` 內），
於父行程預先填好快取。順帶把「RJ45 電口不讀 EEPROM」的判斷移進預熱，讓走 i2c 的
`ethtool -m` 只發生在真的插了模組的網卡上。

★ 根因不是程式碼寫錯，是**只看結構就宣稱修好、沒有實際數過呼叫次數**。改動這一段
MUST 用假 `ethtool` 實測次數。

### `[FIX]` 靜默截斷

`render_ip_routing` 用 `| head -50` 與 `| head -40` 直接截斷 IPv6 路由與鄰居表，
超過上限時無聲丟棄，讀報告的人會以為那就是全部。改為 `print_limited()`，超量時明說
實際總數與未顯示筆數，上限可由 `LIST_LIMIT` 調整。

### `[FIX]` 報告檔權限

報告含 corosync 叢集拓撲與各節點 IP、防火牆規則、`/etc/hosts`、完整路由表。原先僅
倚賴 `REPORT_DIR` 的目錄權限（預設 `/root` 為 0700 尚可），一旦指向 `/var/log` 這類
0755 目錄，同機任何使用者都讀得到。改為以 `umask 077` 建立並顯式 `chmod 600`，
README 的 cron 範例同步加上 `install -d -m 0700`。

### `[ADD]` 新增守備

- 第 10 段：`prime_nic_cache` 的快取行為，含「經 `$( )` 取值不會留下快取」這個**缺陷
  形態本身**，釘住為什麼非得有預熱這一步
- 第 11 段：截斷必須明說截掉多少
- 第 12 段：用自建的假 `ethtool` 與假 sysfs **實跑 `render_physical_nics` 並數呼叫次數**。
  第 10 段守不住「迴圈裡到底有沒有呼叫預熱」，而缺陷正是出在呼叫形態
- 第 13 段：報告檔 0600

### `[ADD]` `skip()` 與 SKIP 計數

平台不支援而驗不了的項目明確標示並單獨計數，**不讓「未驗證」混進「全部通過」**。
本機 `chmod` 設不出 0600（探針實測 `chmod 600`→644、對照 `chmod 400`→444、掛載為
`noacl`），故第 13 段標為 SKIP —— 該項需在 Linux 真機覆驗。

---

## v02.000.000（2026-07-29）

對 v01.000.000 的全面改寫。

### `[FIX]` 媒介判定必然把插了模組的網卡誤判為「光纖」

舊版判準是未錨定的 `LC|SC|MPO|MTP|optical|laser|…` 搭 `-i`，掃的是**整份**
`ethtool -m` 輸出。實測命中片段：

```
Transceiver type : 10G Ethernet: 10G Base-CR   → 命中片段 = "sc"   ← 來自 "Tran(sc)eiver"
Transceiver      : internal                    → 命中片段 = "sc"
Optical diagnostics support : No               → 命中片段 = "Optical"
```

`Transceiver` 這個字必然出現在 `ethtool -m` 輸出裡，`Optical diagnostics support`
連 DAC 也有 ⇒ **只要插了 SFP/QSFP 模組，除非 EEPROM 恰好含 `copper pigtail` 等字被
前一段先攔下，一律判成「光纖」**。

改為只讀 SFF-8472 結構化欄位的**值**（不掃整份 dump），判準優先序：

1. `Port: Twisted Pair` ⇒ RJ45 電口（且不必去讀 SFP EEPROM）
2. 模組接頭是否 RJ45／型別是否含 `BASE-T` ⇒ 銅口 SFP 模組
3. 銅纜與光纖線長欄位（`Length (Copper)` vs `Length (SMF/OM3/…)`）⇒ DAC / 光纖
4. 接頭與線纜技術的**錨定詞**比對

陰性對照：`Twisted Pair`／`0x21 (Copper pigtail)`／`1000baseT` 三者皆不命中光纖判準。

★ 改寫過程中另引入並修掉一個缺陷：**1000BASE-T SFP 銅口模組**因 `Length (Copper)`
填 100m 而被線長判準搶先判成 DAC。修法是把 RJ45 判斷排到線長判準之前（`(2a)` 段）。

### `[FIX]` `read_sysfs()` 用「檔案可讀」代替「讀取成功」

sysfs 有「可讀卻讀取失敗」的情形——介面 admin-down 時讀 `carrier` 回 `EINVAL`。
舊版 `[[ -r "$path" ]]` 為真但 `cat` 失敗 ⇒ 回**空字串**而非預設值，且錯誤訊息外洩；
報告模式帶 `2>&1`，那行 `cat: …: Invalid argument` 會被寫進報告檔。改為判「讀取是否
成功」。

### `[FIX]` 表格 CJK 對齊

`printf %-Ns` 按 byte 補白：「已接線」9 bytes 不補白，而英文表頭「Link」4 bytes 補 4 個
⇒ 顯示寬度 6 vs 8，整欄錯位；「SFP/QSFP 模組」15 bytes 直接撐破 `%-14s`。新增
`str_width()` / `pad()` 依**顯示寬度**補白（UTF-8 下 CJK 全形字 3 bytes / 1 char 且佔
2 欄，故 顯示寬度 = 字元數 + (byte 數 − 字元數) / 2）。

### `[FIX]` 其他

- `ethtool` 呼叫次數：舊版每張網卡 7 次（`get_port_type` 內就 3 次，含走 i2c 的
  `ethtool -m`）。改為結構上每種輸出各取一次。
  ⚠ 本項當時宣稱「並快取」是錯的，快取實際未生效——見 v02.000.001。
- `report_vlans()` 未檢查 `bridge` 指令是否存在（`show_vlans` 有檢查），不一致且會讓
  command not found 進報告；`show_vlans` 另把 `bridge vlan show` 跑了兩次。
- `sample_traffic()` 末尾的 `((… < 0))` 使函式在正常情況回 rc=1。

### `[REFACTOR]` 畫面與報告合併為單一實作

舊版 `show_*` 與 `report_*` 是兩套幾乎相同的碼，欄位已有不同步風險。合併為單一
`render_*`，靠 `USE_COLOR` 切換著色。

### `[ADD]` 新增盤查項目

Open vSwitch、PVE SDN、VM/CT 網卡對應（`tap`/`veth` ←→ VMID）、VLAN 對帳、corosync
環網、pve-firewall、路由／DNS／`hosts`／鄰居、IPv6、MTU、`carrier_changes`、錯誤與
丟包統計、韌體版本、NUMA。

### `[ADD]` 非互動介面與內建自檢

`--report`（可排 cron）／`--self-test`／`--help`／`--version`；自檢對有已知正確答案的
判定邏輯做比對。

### `[CHANGE]` 版本號改用 `XX.XXX.XXX` 慣例

舊版標示為 `1.0.0`。

---

## v01.000.000

初版（原始碼中標示為 `1.0.0`）。互動選單，涵蓋實體網卡狀態、Bond、Linux Bridge、
VLAN、LLDP、`/etc/network/interfaces`、網卡 LED 定位，以及完整報告輸出。
